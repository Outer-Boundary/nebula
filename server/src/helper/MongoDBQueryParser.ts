// export default function mongoDBQueryParser(query: QueryString.ParsedQs, allowedFields: string[], options?: string[]): {} {
//   Object.entries(query).forEach(([key, value]) => console.log(key, value));
// }

export default {};
